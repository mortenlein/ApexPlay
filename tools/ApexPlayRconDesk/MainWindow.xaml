<Window x:Class="ApexPlayRconDesk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:ApexPlayRconDesk.Models"
        mc:Ignorable="d"
        Title="ApexPlay RCON Desk"
        Height="860"
        Width="1260"
        MinHeight="740"
        MinWidth="1080"
        KeyDown="OnWindowKeyDown">
  <Grid Background="#101418">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="2*" />
    </Grid.RowDefinitions>

    <Border Grid.Row="0" Margin="12" Padding="12" Background="#141b22" CornerRadius="8" BorderBrush="#26313c" BorderThickness="1">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="3*" />
          <ColumnDefinition Width="2*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
          <TextBlock Text="Server" Foreground="#8fa0b2" Margin="0,0,10,0" VerticalAlignment="Center" />
          <ComboBox x:Name="ServerComboBox" Width="320" DisplayMemberPath="Name" SelectionChanged="OnServerSelected" />
          <Button x:Name="AddServerButton" Content="Add" Margin="10,0,0,0" Padding="10,6" Click="OnAddServer" />
          <Button x:Name="SaveServerButton" Content="Save" Margin="8,0,0,0" Padding="10,6" Click="OnSaveServer" />
          <Button x:Name="DeleteServerButton" Content="Delete" Margin="8,0,0,0" Padding="10,6" Click="OnDeleteServer" />
        </StackPanel>

        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
          <TextBlock Text="Host" Foreground="#8fa0b2" Margin="0,0,8,0" VerticalAlignment="Center" />
          <TextBox x:Name="HostTextBox" Width="140" Margin="0,0,12,0" />
          <TextBlock Text="Port" Foreground="#8fa0b2" Margin="0,0,8,0" VerticalAlignment="Center" />
          <TextBox x:Name="PortTextBox" Width="60" Margin="0,0,12,0" />
          <TextBlock Text="Server PW" Foreground="#8fa0b2" Margin="0,0,8,0" VerticalAlignment="Center" />
          <PasswordBox x:Name="ServerPasswordBox" Width="110" Margin="0,0,12,0" />
          <TextBlock Text="RCON" Foreground="#8fa0b2" Margin="0,0,8,0" VerticalAlignment="Center" />
          <PasswordBox x:Name="PasswordBox" Width="110" />
        </StackPanel>

        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0,0,0">
          <Button x:Name="ConnectButton" Content="Connect" Padding="14,8" Click="OnConnect" />
          <Button x:Name="DisconnectButton" Content="Disconnect" Margin="8,0,0,0" Padding="14,8" Click="OnDisconnect" IsEnabled="False" />
        </StackPanel>
      </Grid>
    </Border>

    <Grid Grid.Row="1" Margin="12,0,12,12">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="2*" />
        <ColumnDefinition Width="3*" />
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Margin="0,0,12,0" Padding="12" Background="#141b22" CornerRadius="8" BorderBrush="#26313c" BorderThickness="1">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
          </Grid.RowDefinitions>

          <StackPanel Grid.Row="0" Orientation="Horizontal">
            <TextBlock Text="Quick Actions" Foreground="#dbe7f3" FontSize="16" FontWeight="SemiBold" />
            <TextBlock Text=" (click or hotkey)" Foreground="#8fa0b2" FontSize="12" Margin="8,4,0,0" />
          </StackPanel>

          <TextBlock Grid.Row="1" x:Name="HotkeyHintText" Text="Hotkeys: F1/F2/F3/F4, Ctrl+1/Ctrl+2/Ctrl+K/Ctrl+E"
                     Foreground="#8fa0b2" Margin="0,8,0,12" />

          <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ActionItemsControl">
              <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                  <WrapPanel />
                </ItemsPanelTemplate>
              </ItemsControl.ItemsPanel>
              <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type models:ActionPreset}">
                  <Button Width="220" Height="74" Margin="0,0,10,10" Padding="10" Tag="{Binding}" Click="OnActionButtonClick">
                    <StackPanel>
                      <TextBlock Text="{Binding Name}" FontWeight="SemiBold" />
                      <TextBlock Text="{Binding Hotkey}" FontSize="11" Foreground="#78889b" Margin="0,4,0,0" />
                      <TextBlock Text="{Binding CommandScript}" FontSize="10" Foreground="#66788c" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                  </Button>
                </DataTemplate>
              </ItemsControl.ItemTemplate>
            </ItemsControl>
          </ScrollViewer>
        </Grid>
      </Border>

      <Border Grid.Column="1" Padding="12" Background="#141b22" CornerRadius="8" BorderBrush="#26313c" BorderThickness="1">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
          </Grid.RowDefinitions>

          <TextBlock Text="Macro Editor" Foreground="#dbe7f3" FontSize="16" FontWeight="SemiBold" />
          <TextBox Grid.Row="1" x:Name="MacroEditorTextBox" AcceptsReturn="True" TextWrapping="Wrap"
                   VerticalScrollBarVisibility="Auto" Margin="0,10,0,10"
                   Background="#0f151c" Foreground="#dbe7f3" BorderBrush="#26313c"
                   Text="say [ADMIN] Live in 10 seconds&#10;mp_restartgame 10" />
          <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SendMacroButton" Content="Run Macro" Padding="14,8" Click="OnSendMacro" />
          </StackPanel>
        </Grid>
      </Border>
    </Grid>

    <Border Grid.Row="2" Margin="12,0,12,12" Padding="12" Background="#141b22" CornerRadius="8" BorderBrush="#26313c" BorderThickness="1">
      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />
          <RowDefinition Height="*" />
          <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Console / Output" Foreground="#dbe7f3" FontSize="16" FontWeight="SemiBold" />
        <ListBox Grid.Row="1" x:Name="LogListBox" Margin="0,10,0,10" Background="#0f151c" BorderBrush="#26313c" Foreground="#dbe7f3" />
        <DockPanel Grid.Row="2">
          <TextBox x:Name="ManualCommandTextBox" MinHeight="32" VerticalAlignment="Center" Margin="0,0,8,0" />
          <Button Content="Copy Join Link" Width="120" Margin="0,0,8,0" Click="OnCopyJoinLink" />
          <Button Content="Send Command" Width="120" Click="OnSendManualCommand" />
        </DockPanel>
      </Grid>
    </Border>
  </Grid>
</Window>
